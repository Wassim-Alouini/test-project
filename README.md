# test-project

## Git commands

- Creating new branch `side`

```
git branch side
```

- Switching to branch `side`

```
git checkout side
```

- Creating new branch `side` and switching to it

```
git checkout -b side
```